<div class="map-wrapper">
  <input <%= 'checked' if @show_cluster %> class="map-toggle" type="checkbox" name="my-checkbox" data-on-text="Cluster" data-off-text="Marker">
  <div id="map-canvas">
  </div>
</div>

<% content_for :footer do %>

  <%= javascript_tag do %>
    //window.kmlUrl= "<%= root_url %>permits.kml";
    window.kmlUrl= "http://freeparkingfor.me/permits.kml";
    window.jsonUrl= "<%= root_url %>permits.json";
  <% end %>

<% end %>
